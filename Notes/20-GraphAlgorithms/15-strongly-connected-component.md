# Strongly Connected Component

**A maximal set `S` of vertices such that if vertex `u` and vertex `v` are in `S`, then there is a path from vertex `u` to vertex `v` and there is a path from vertex `v` to vertex `u`.**


<p align="center">
  <img src="https://github.com/thespcrewroy/CSCI4470-Algorithms/blob/main/Notes/assets/sccexample.png" alt="Strongly Connected Component Example" height="700" width="700" />
</p>